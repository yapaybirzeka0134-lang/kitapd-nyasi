"use client";
import Link from "next/link"; import { Moon, Search, Sun } from "lucide-react"; import { useTheme } from "next-themes";
export function Header() { const { resolvedTheme, setTheme } = useTheme(); return <header className="sticky top-0 z-40 border-b bg-[rgb(var(--bg)/.9)] backdrop-blur"><div className="container flex h-16 items-center gap-5"><Link href="/" className="text-lg font-black tracking-tight"><span className="text-violet">Kitap</span>Dünyası</Link><nav className="hidden flex-1 items-center justify-center gap-5 text-sm font-medium md:flex">{[["/","Ana Sayfa"],["/discover","Keşfet"],["/discover?type=roman","Romanlar"],["/discover?type=book","Kitaplar"],["/discover","Türler"],["/author/leyla-arin","Yazarlar"]].map(([href,label])=><Link key={label} href={href}>{label}</Link>)}</nav><div className="ml-auto flex items-center gap-2"><Link href="/search" aria-label="Ara"><Search size={19}/></Link><button aria-label="Temayı değiştir" onClick={()=>setTheme(resolvedTheme === "dark" ? "light" : "dark")}>{resolvedTheme === "dark"?<Sun size={19}/>:<Moon size={19}/>}</button><Link className="hidden text-sm sm:block" href="/login">Giriş Yap</Link><Link className="rounded-lg bg-violet px-3 py-2 text-sm font-semibold text-white" href="/register">Kayıt Ol</Link></div></div></header> }
