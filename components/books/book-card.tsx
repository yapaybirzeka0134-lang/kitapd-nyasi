import Image from "next/image"; import Link from "next/link"; import { Eye, Star } from "lucide-react"; import type { Book } from "@/types";
export function BookCard({book}: {book: Book}) { return <Link href={`/book/${book.slug}`} className="group overflow-hidden rounded-xl border bg-[rgb(var(--card))] transition hover:-translate-y-1 hover:shadow-xl"><div className="relative aspect-[3/4]"><Image src={book.cover} alt={`${book.title} kapağı`} fill className="object-cover" sizes="(max-width: 640px) 50vw, 25vw"/></div><div className="p-3"><p className="font-bold leading-tight">{book.title}</p><p className="mt-1 text-sm text-zinc-500">{book.author} · {book.category}</p><div className="mt-3 flex gap-3 text-xs text-zinc-500"><span className="flex items-center gap-1"><Star size={13} className="fill-gold text-gold"/>{book.rating}</span><span className="flex items-center gap-1"><Eye size={13}/>{(book.views/1000).toFixed(1)}K</span></div></div></Link> }
