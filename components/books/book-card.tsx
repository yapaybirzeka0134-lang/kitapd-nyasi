import Image from "next/image"; import Link from "next/link"; import { BookOpen, Calendar, UserRound } from "lucide-react"; import type { Book } from "@/types";
export function BookCard({book}: {book: Book}) { return <Link href={`/book/${book.slug}`} className="group overflow-hidden rounded-xl border bg-[rgb(var(--card))] transition hover:-translate-y-1 hover:shadow-xl"><div className="relative aspect-[3/4] bg-zinc-100 dark:bg-zinc-900">{book.cover ? <Image src={book.cover} alt={`${book.title} kapağı`} fill className="object-cover" sizes="(max-width:640px) 50vw, 25vw"/> : <BookOpen className="absolute inset-0 m-auto text-zinc-400" size={42}/>}</div><div className="p-3"><p className="line-clamp-2 font-bold leading-tight">{book.title}</p><p className="mt-1 line-clamp-1 text-sm text-zinc-500">{book.author}</p><div className="mt-3 flex gap-3 text-xs text-zinc-500"><span className="flex items-center gap-1"><UserRound size={13}/>{book.category}</span>{book.publishedYear&&<span className="flex items-center gap-1"><Calendar size={13}/>{book.publishedYear}</span>}</div></div></Link> }
