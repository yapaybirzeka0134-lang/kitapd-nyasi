import { BookCard } from "@/components/books/book-card"; import { searchBooks } from "@/lib/books";
export default async function Search({searchParams}:{searchParams:Promise<{q?:string}>}) { const {q=""}=await searchParams; const books=q?await searchBooks(q):[]; return <main className="container py-10"><h1 className="text-3xl font-black">Arama</h1><form className="mt-6 flex gap-2"><input name="q" defaultValue={q} placeholder="Kitap, yazar, tür veya ISBN ara" className="min-w-0 flex-1 rounded-lg border bg-transparent px-4 py-3"/><button className="rounded-lg bg-violet px-5 font-bold text-white">Ara</button></form>{q&&<p className="mt-6 text-zinc-500">“{q}” için {books.length} sonuç</p>}<div className="mt-6 grid grid-cols-2 gap-4 md:grid-cols-4">{books.map(x=><BookCard key={x.id} book={x}/>)}</div></main> }
