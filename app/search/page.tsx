import { BookCard } from "@/components/books/book-card"; import { books } from "@/lib/data";
export default async function Search({searchParams}:{searchParams:Promise<{q?:string}>}) { const {q=""}=await searchParams; const result=books.filter(x=>`${x.title} ${x.author} ${x.category}`.toLocaleLowerCase("tr").includes(q.toLocaleLowerCase("tr"))); return <main className="container py-10"><h1 className="text-3xl font-black">Arama</h1><form className="mt-6"><input name="q" defaultValue={q} placeholder="Kitap, yazar, tür veya ISBN ara" className="w-full rounded-lg border bg-transparent px-4 py-3"/></form>{q&&<p className="mt-6 text-zinc-500">“{q}” için {result.length} sonuç</p>}<div className="mt-6 grid grid-cols-2 gap-4 md:grid-cols-4">{result.map(x=><BookCard key={x.id} book={x}/>)}</div></main> }
