'use client'; import { createClient } from '@/lib/supabase/client'; export default function Login(){async function signIn(data:FormData){const supabase=createClient();const {error}=await supabase.auth.signInWithPassword({email:String(data.get('email')),password:String(data.get('password'))});if(error)alert(error.message);else location.assign('/admin')}return <main className="container max-w-md py-16"><h1 className="text-3xl font-black">Yönetici Girişi</h1><form action={signIn} className="mt-8 grid gap-3"><input required name="email" type="email" className="rounded-lg border bg-transparent p-3" placeholder="E-posta"/><input required name="password" type="password" className="rounded-lg border bg-transparent p-3" placeholder="Şifre"/><button className="rounded-lg bg-violet p-3 font-bold text-white">Giriş yap</button></form></main>}
