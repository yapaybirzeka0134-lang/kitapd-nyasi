import { notFound } from 'next/navigation';import { createClient } from '@/lib/supabase/server';import { BookForm } from '@/components/admin/book-form';export default async function Edit({params}:{params:Promise<{id:string}>}){const {id}=await params;const supabase=await createClient();const {data:book}=await supabase.from('books').select('id,title,slug,author,description,category,status').eq('id',id).single();if(!book)notFound();return <main className="container max-w-2xl py-10"><h1 className="mb-8 text-3xl font-black">Kitabı Düzenle</h1><BookForm book={book}/></main>}
