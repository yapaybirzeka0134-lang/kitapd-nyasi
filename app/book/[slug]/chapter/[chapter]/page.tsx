import Link from "next/link"; import { notFound } from "next/navigation"; import { getBook } from "@/lib/books";
export default async function Chapter({params}:{params:Promise<{slug:string;chapter:string}>}){const {slug}=await params;const book=await getBook(slug);if(!book)notFound();return <main className="min-h-screen"><header className="border-b py-3"><div className="container flex justify-between text-sm"><Link href={`/book/${slug}`}>← Kitaba dön</Link><span className="font-bold">{book.title}</span></div></header><article className="mx-auto max-w-2xl px-5 py-14 font-serif text-lg leading-9"><h1 className="mb-10 text-center text-3xl font-black">Okuma erişimi</h1><p>Bu eser için hak sahibi tarafından sağlanmış, yayınlanabilir bir metin dosyası bulunmuyor.</p><p className="mt-6">KitapDünyası telifli metinleri üçüncü taraf sitelerden çekmez. Kamu malı veya lisanslı içerik eklendiğinde okuyucu etkinleştirilir.</p></article></main>}
